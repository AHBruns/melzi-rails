<%= render DataTables::DataCardComponent.new do |comp|
  comp.data_row(key: "Id", value: @object.id)
  comp.data_row(key: "Created", value: "#{time_ago_in_words(@object.created_at)} ago")
  comp.data_row(key: "Last Updated", value: "#{time_ago_in_words(@object.updated_at)} ago")
end %>
