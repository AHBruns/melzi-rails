<%= if @object.present?
      @form.collection_select(
        @object,
        @method,
        @collection,
        @value_method,
        @text_method,
        @options,
        @html_options.merge({ class: @class }))
    else
      @form.collection_select(
        @method,
        @collection,
        @value_method,
        @text_method,
        @options,
        @html_options.merge({ class: @class }))
    end %>
