<%= if @object.present?
     @form.select(
       @object,
       @method,
       @choices,
       @options,
       @html_options.merge({ class: @class }))
   else
     @form.select(
       @method,
       @choices,
       @options,
       @html_options.merge({ class: @class }))
   end %>