<%= if @object.present?
     @form.file_field(
       @object,
       @method,
       @options.merge({ class: @class }))
   else
     @form.file_field(
       @method,
       @options.merge({ class: @class }))
   end %>