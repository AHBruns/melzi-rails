<%= if @object.present?
      @form.check_box(
        @object,
        @method,
        @options.merge({ class: @class }),
        @checked_value,
        @unchecked_value)
    else
      @form.check_box(
        @method,
        @options.merge({ class: @class }),
        @checked_value,
        @unchecked_value)
    end %>