<%= form_with(model: @submission) do |form| %>
  <%= render Forms::BaseFormComponent.new(form: form, errors: @submission.errors) do %>
    <%= render Forms::SubmissionFieldsetComponent.new(form: form, submission: @submission, current_user: @current_user) %>
  <% end %>
<% end %>
